# 3/27（金）｜独立課題②の設計を考える

**所要目安**: 6〜8時間  
**AIなしで取り組む。帰宅後にClaudeと設計レビューをする。**

コードは書かない。設計だけを考える日。

---

## 課題

### Part 1：コンポーネント設計

```
Q1: どんなコンポーネントに分割するか（App.tsxだけで全部書くのはNG）
Q2: 各コンポーネントの責任は何か
Q3: コンポーネント間でどうデータを渡すか（どこで状態を持つか）
```

### Part 2：状態設計

```
Q1: useState で管理すべき状態は何個あるか。型は何か
Q2: フィルターの絞り込みロジックはどこで実行するか
Q3: データはどこから取得するか（モックJSONのパスは？）
```

### Part 3：モックデータの作成

`frontend/project3-dashboard/public/mock/ads.json` を作成する。
5件以上のデータを用意する（フィールド名は `src/types/ad.ts` の型と合わせる）。

### Part 4：仕様確認チェックリスト

```
表示要件：
- [ ] 広告一覧をテーブルで表示する
- [ ] CTRを計算して表示する（小数点2桁・%）
- [ ] 広告名でフィルタリングできる
- [ ] 0件のときは「該当なし」を表示する
- [ ] ローディング・エラー状態を実装する

型要件：
- [ ] 全コンポーネントのpropsに型がある
- [ ] any を使わない
```

---

## 帰宅後にClaudeに見せるもの

- Part 1・2・3 の設計メモ
- 設計で悩んでいる点・迷っている点
