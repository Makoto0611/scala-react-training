# 3/20（金）｜Option・独立課題①の設計

**所要目安**: 6〜8時間  
**参照書籍**: 実践Scala入門 第3章「Option」  
**AIなしで取り組む。帰宅後にClaudeと設計レビューをする。**

---

## 今日読む箇所（30分）

実践Scala入門 第3章「Option ── 『値がないかもしれない』を表す」全体を読む。

読みながら意識すること：
- `Option` はなぜ `null` より安全なのか
- `Some` と `None` の使い分け
- `getOrElse` / `map` / `flatMap` / `match` でどう扱うか

---

## 課題

### Part 1：Option を使ってみる

```
関数名： findAd
引数：   adId: String, ads: List[Ad]
戻り値： Option[Ad]

・ads の中に adId と一致する Ad があれば Some(ad) を返す
・なければ None を返す
```

`match` で処理して、見つかった場合・見つからない場合を出力する。

### Part 2：CTR 計算関数の設計

```
CTR = clicks / impressions × 100
問題：impressions が 0 のときゼロ除算が発生する

方針A：if式でゼロをチェックする
方針B：Option を使って安全に処理する

どちらが Scala らしいか、理由とともにコメントで書く。
実装はまだしなくてよい。設計だけ。
```

### Part 3：課題①の全体設計をメモする（日本語で）

```
1. 必要な case class は何か（昨日までのものを流用できるか）
2. 処理の流れを書く（JSONを読む → ? → ? → CTRを計算 → 出力）
3. adId ごとに集計するには何を使うか
4. クリックが0件の広告（ad_003）はどう処理するか
5. 出力のフォーマットをどう作るか
```

帰宅後、この設計メモをClaudeに見せてレビューしてもらう。

---

## 完了条件

- [ ] `findAd` 関数が動作する
- [ ] Part 2 の考察コメントが書かれている
- [ ] Part 3 の設計メモが日本語で書かれている

---

## 詰まったときの調べ方

| 詰まりポイント | 調べるキーワード / 逆引きレシピ番号 |
|-------------|-------------------------------|
| Option の基本 | 実践Scala入門 第3章 / レシピ050 |
| List から条件で1件取得 | `Scala find` / レシピ108 |
| Option を match で処理 | `Scala Option match` |
